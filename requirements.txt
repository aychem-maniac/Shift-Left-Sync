# Shift-Left-Sync 실행용 라이브러리 목록
# 이 파일은 프로젝트 실행에 필요한 외부 Python 라이브러리를 정리한다.
# 설치 명령어:
# pip install -r requirements.txt


# HTTP 요청 및 응답 데이터 수집용 라이브러리
# 대상 URL에 요청을 보내고, 응답 헤더, 쿠키, HTML 내용을 수집할 때 사용한다.
requests


# Nmap 포트 스캔 연동용 라이브러리
# Python 코드에서 Nmap 실행 결과를 다루기 위해 사용한다.
# 주의: 이 라이브러리만 설치한다고 Nmap 프로그램 자체가 설치되는 것은 아니다.
# 별도로 Nmap이 PC 또는 서버에 설치되어 있어야 한다.
python-nmap


# OWASP ZAP API 연동용 라이브러리
# Python 코드에서 OWASP ZAP과 연결하고 스캔 결과를 가져올 때 사용한다.
# 주의: 이 라이브러리만 설치한다고 OWASP ZAP 프로그램 자체가 설치되는 것은 아니다.
# 별도로 OWASP ZAP이 설치되어 있고 실행 중이어야 한다.
zaproxy