# sls_scanner/normalizers/nmap_normalizer.py


def normalize_nmap_result(raw_nmap_result: dict) -> list[dict]:
    # 나중에 Nmap 원본 결과를 공통 포트 결과 형식으로 바꿀 자리다.
    print("[INFO] Nmap normalization placeholder")

    # 지금은 빈 리스트를 반환한다.
    return []