# sls_scanner/normalizers/zap_normalizer.py


def normalize_zap_result(raw_zap_result: dict) -> list[dict]:
    # 나중에 ZAP Alert 결과를 공통 취약점 결과 형식으로 바꿀 자리다.
    print("[INFO] ZAP normalization placeholder")

    # 지금은 빈 리스트를 반환한다.
    return []