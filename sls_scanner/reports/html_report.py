# sls_scanner/reports/html_report.py


def generate_html_report(
    target: str,
    port_results: list[dict],
    findings: list[dict]
) -> str:
    # 나중에 HTML 리포트를 생성하는 코드가 들어갈 자리다.
    print("[INFO] HTML report generation placeholder")

    # 지금은 더미 리포트 경로를 반환한다.
    return "results/reports/report.html"